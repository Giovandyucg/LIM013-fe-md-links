

//trabajar las rutas,directorio y la option validate
// const path = require('path');
// const fs = require('fs');
// const isAbsolutePath = (route) => path.isAbsolute(route);
// const transInAbsolute = (route) => path.resolve(route);
// module.exports = { isAbsolutePath ,transInAbsolute};
function suma(a, b) {
  return a + b;
}

export default suma;
// exports.default = _default;